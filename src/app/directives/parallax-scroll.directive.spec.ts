import { ParallaxScrollDirective } from './parallax-scroll.directive';

describe('ParallaxScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxScrollDirective();
    expect(directive).toBeTruthy();
  });
});
